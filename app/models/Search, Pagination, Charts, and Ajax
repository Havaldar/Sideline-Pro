Search:
	Add a text field to the view -> <%= form_tag with a get method%>
	Modify the controller to load just the projects like the ones entered
	Use the model to filter down the data -> model_name.where "name LIKE %?%", params[:q] 
Pagination:
	will_paginate (easy)
Charts:
	lazy_high_charts 
Ajax:
	