<%= form_for(team, {:url => "/teams"}) do |f| %>
  <%= f.label :name %>:
  <%= f.text_field :name %>
  <br/>
  <div class="field">
    <%= f.label "Team Photo:" %><br />
    <%= f.file_field :avatar %><br/>
  </div>
  <%= f.submit %>

<% end %>