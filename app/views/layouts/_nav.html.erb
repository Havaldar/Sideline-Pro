<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation" id="navbar">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navtabs">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <div class="navbar-brand hvr-underline-reveal" id="logo"><%= link_to 'SIDELINE PRO', "/users/#{current_user.id}" %></div>
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                <li>
                    <%= form_tag "/teams", method: 'GET', id: 'teams_search', class: "navbar-form navbar-left", role: "search" do %>
						<div class="form-group">
							<%= text_field_tag 'q', params[:q], class: "form-control", placeholder: "Search Teams" %>
							<span class="glyphicon glyphicon-search" aria-hidden="true"></span>
						</div>
				<% end %>
				</li>
			</ul>
			<ul class="nav navbar-nav navbar-right" id="leftnavtabs">	
                <li role="presentation" class="hvr-fade"><%= link_to 'PROFILE', "/users/#{current_user.id}"%></li>
				<li role="presentation" class="hvr-fade"><%= link_to 'TEAMS', teams_path%></li>			
				<li role="presentation" class="hvr-fade"><%= link_to 'SIGN OUT', destroy_user_session_path, method: :delete %></li>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container -->
</nav>
